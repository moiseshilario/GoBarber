{% for hour in available %}
  <label for="time-{{hour.value}}" class="{% if hour.available === false %}disabled{% endif %}">
    {% if hour.available === true %}
      <input type="radio" name="date" value="{{ hour.value }}" id="time-{{hour.value}}">
    {% endif %}

    {{ hour.time }}
  </label>
{% endfor %}
